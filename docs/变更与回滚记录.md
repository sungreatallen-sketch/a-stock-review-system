# 变更与回滚记录（CHANGELOG）

> 文档编号：CHG-001 ｜ 版本：v1.2 ｜ 用途：记录全部变更 + 提供可回滚索引（便于日后回滚）
> 关联：REQ-001 / ARCH-001 / ISSUE-001 / execution_rules.yaml

## 1. 版本基线
| 版本 | 日期 | 内容 | 提交 |
|---|---|---|---|
| v1.0 | 2026-08-12 | 需求/架构/规则工程化基线（问题1-5完成，待封装 tag） | c71dacb → 746ad14 |
| v1.1 | 2026-08-12 | 问题修复记录 + 需求升级 RQ-1.1~1.10 + R27 | 167c924 |
| v1.2 | 2026-08-12 | R21/R24/R25 实现 + 昨日复盘显眼化 + 因子库 R28 + 服务模板修复 | 2d86b88 / 575604b / d783cac / 746ad14 |

## 2. 变更记录（含回滚索引）
> 回滚方法：`git revert <hash>` 撤销代码；`git checkout <hash> -- <file>` 还原单文件；改代码后需重启对应服务。

| 变更ID | 内容 | 提交 | 回滚方法 |
|---|---|---|---|
| C01 | M1 基础：复盘采集/交叉验证/HTML/Web服务/飞书入口 | 985afbe | git revert 985afbe |
| C02 | M2 候选池+打分+量比过滤+回测+预测 | 7f15923 | git revert 7f15923 |
| C03 | M3 消息面+DeepSeek研判+模拟盘跟踪 | 2be8b06 | git revert 2be8b06 |
| C04 | M4 模拟盘自动化（自动结算+飞书结算+定时） | afc573d | git revert afc573d |
| C05 | 修复：飞书回复被系统代理拦截→直连+重试 | 81ea312 | git revert 81ea312 |
| C06 | 修复：消息去重+确认消息移后台+同日复用+NO_PROXY+.local | b2f7bdd | git revert b2f7bdd |
| C07 | 修复：回复去重键=(消息ID,类型) | 19d188e | git revert 19d188e |
| C08 | 统一复盘/预测为完整报告+HTML预测名称修复 | db227ee | git revert db227ee |
| C09 | 推荐跟踪：卡片昨日结算+HTML跟踪区块+ego结算兜底 | e599a15 | git revert e599a15 |
| C10 | 假设库（16条 hypothesis） | ee3cc01 | git revert ee3cc01 |
| C11 | H003/H001-16 独立回测（不改策略代码） | de04a57/fa5e852/4e43797 | git revert 对应提交 |
| C12 | 修复：手机打不开HTML→多链接策略 | 3bdfb0c | git revert 3bdfb0c |
| C13 | 修复：MCP自动发现端口+push2delay兜底+报告完整才复用+--force | de28108 | git revert de28108 |
| C14 | 修复：卡片昨日复盘显示条件 | 94379e1 | git revert 94379e1 |
| C15 | 评估双口径（开盘+收盘） | e13a9d2 | git revert e13a9d2 |
| C16 | 规则系统（execution_rules+RULES+合规自检+「规则」命令） | 526a3fe | git revert 526a3fe |
| C17 | 预测六步流程固化 R21-R26 + LLM截断回退 | 60edcd5 | git revert 60edcd5 |
| C18 | 问题1-2：工程文档(REQ/ARCH/ENG)+问题记录+规则v1.1 | c71dacb/167c924 | git revert 对应提交 |
| C19 | 问题3：R21(10日窗口)/R24(龙虎榜)/R25(买卖计划) | 2d86b88 | git revert 2d86b88 |
| C20 | 问题4：HTML昨日复盘上移+卡片顶部摘要 | 575604b | git revert 575604b；**并重启 com.ashare.server** |
| C21 | 问题5：因子库(46因子)+R28 | d783cac | git revert d783cac |
| C22 | 问题4再修复：报告服务缓存旧HTML模板→重启server+运维约定 | 746ad14 | 代码无需回滚（运维问题）；回滚方式=重启 server 后还原旧模板则改 html_report.py |
| C23 | 评估口径改为昨收买→今收卖（收盘-收盘），命中率按此计算；卡片/HTML 显示昨收/今收/收益 | (v1.4) | 回滚：git revert 本版本；重结算 .venv/bin/python run_cli.py track auto |
| C24 | 手机打不开HTML与VPN相关：lan_ip 改为优先默认路由物理网卡，排除 utun/tun 虚拟网卡 | (v1.4) | 回滚：git revert 本版本（还原 socket 取IP）；重启 bot/server |
| C25 | 报告图片直发飞书：复盘后自动生成手机宽度长图(3段)直接发到聊天，不依赖局域网/IP/VPN | (v1.4) | 回滚：git revert 本版本；重启 bot |
| C26 | 修复 C25 引入的 NameError('p')：_full_report_and_reply 引用未定义变量，导致整条复盘失败 | (v1.4) | 回滚：git revert 本版本；重启 bot |
| C27 | 图片上传改为直接 requests 调飞书 API；补充应用 im:resource 权限并发布新版本，图片直发功能打通 | (v1.4) | 回滚：git revert 本版本；权限如需撤销到控制台处理 |
| C28 | 图片卡片改为新消息直发聊天（requests 直调 message API，不再用回复），确保用户可见 | (v1.4) | 回滚：git revert 本版本；重启 bot |
| C29 | 后台数据积累文件 data/recommendation_history.json（全部已结算记录+日/累计/分股/按月分析），每次结算自动更新+CLI track history；HTML 跟踪表只显示最近一天（不累计展示） | (v1.1.0) | 回滚：git revert 本版本；重启 bot/server |
| C30 | 接入新框架前备份点：提交当前工作区(含RSI接入分析文档/最新数据/报告) ee8fd5c；外部备份 /Users/yage/ashare-backups/2026-08-18_pre-framework/（git bundle+全量/轻量tar包+requirements+脚本+launchd+日志） | ee8fd5c | 回滚：见 docs/变更与回滚记录.md 第5节 与 备份目录 RESTORE.md |
| C31 | RSI Framework 基础 Application 接入 Phase 1-9 完成：rsi_agent.py(AShareAgent) + rsi_app.py(Adapter入口) + rsi_tools.py(Tool适配) + rsi_memory.py(Memory适配) + rsi_evaluator.py(Evaluator适配) + bot.py(RSI开关) + config.yaml(rsi.enabled:false) + requirements.txt(RSI依赖) | (待提交) | 回滚：git revert 本提交；或将 config.yaml 的 rsi.enabled 改为 false 即回到 Legacy Flow |
| C32 | RSI Feedback Loop 闭环：rsi_evaluator v2(单次预测评估) + rsi_memory v2(Experience存储+去重) + rsi_feedback(settle->eval->memory桥接) + rsi_app集成触发 | (待提交) | 回滚：git revert 本提交；或 rsi.enabled:false 关闭 |
| C33 | RSI Insight Analyzer: rsi_insight.py(结构化Insight模型+统计分析引擎) + data/insights.json(持久化) + 样本门槛(≥3) + 去重(md5) | (待提交) | 回滚：git revert 本提交 |
| C34 | RSI Candidate Strategy: rsi_candidate.py(Insight→Candidate生成/回测/基准对比) + candidate/目录 + 数据泄漏控制(discovery/validation分离) + 样本门槛(MIN_TRADES=10) | (待提交) | 回滚：git revert 本提交; 删除candidate/ |
| C35 | RSI Walk-Forward Validation: rsi_validation.py(WalkForwardValidator+统计检验+鲁棒性+状态更新) + data/validation/ + Wilson CI/z-test | (待提交) | 回滚: git revert |
| C36 | RSI Shadow Mode: rsi_shadow.py(ShadowRunner+ShadowEvaluator+ShadowComparator) + shadow_predictions/shadow_results表 + prediction_mode隔离 + 数据源区分 | (待提交) | 回滚: git revert |
| C37 | safe_json 5层容错增强: 修复 DeepSeek reasoning_content 混入 content 导致研判解析失败 | 597eda6 | 回滚: git revert 597eda6 |
| C38 | R29合规自检修复: _attach_compliance pre变量bug + R29从空壳改为实际校验 + git pre-commit hook强制检查 | (待提交) | 回滚: git revert |
| C39 | 涨停股过滤修复: strategy.py过滤涨停+scoring.py扣分-5→-50+backtest.py跳过涨停 | (待提交) | 回滚: git revert |
| C40 | 涨停不可交易硬约束规范化: strategy.py(STRATEGY_VERSION=v1.1 + filter_reason=limit_up_untradable + self.filtered保留数据) + daily.py/workflow.py(strategy_version+filtered_out) + scoring.py回滚涨停扣分-50→-5 | (待提交) | 回滚: git revert |

## 3. 回滚操作手册
### 3.1 代码回滚
```bash
cd /Users/yage/Documents/我的预测系统
git log --oneline | head -30        # 查提交
git revert <hash>                   # 撤销某次变更（生成新的反向提交）
# 或仅还原某文件
git checkout <hash> -- app/predict/scoring.py
```
### 3.2 服务重启（改代码/模板后必须）
```bash
launchctl kickstart -k gui/$(id -u)/com.ashare.bot      # 机器人
launchctl kickstart -k gui/$(id -u)/com.ashare.server   # 报告服务（HTML模板变更必重启）
```
### 3.3 数据回滚（SQLite）
```bash
# 撤销某日错误结算
.venv/bin/python -c "
import sqlite3
from app.config import paths
c = sqlite3.connect(paths()['data'] / 'a_share.db')
c.execute('DELETE FROM prediction_results WHERE date=?', ('2026-08-12',))
c.commit()"
# 重新结算：.venv/bin/python run_cli.py track auto
```
### 3.4 规则回滚
- execution_rules.yaml 版本字段回退 + git revert 对应规则变更提交
- RULES.md 同步

## 4. 防复发约定（I24 教训）
- HTML 模板/前端变更后**必须重启 com.ashare.server**
- 任何代码变更后重启对应服务；用 curl 验证服务器实际输出
  `curl -s http://127.0.0.1:8787/report/<date> | grep 关键片段`

## 5. 备份点（接入新框架前，2026-08-18）
- 目的：接入 RSI Agent Framework 前的完整可回滚快照，防止新框架污染/损坏现有项目。
- Git 提交：`ee8fd5c`（备份点，基于 v1.1.0 之上，包含最新数据/报告/RSI 接入分析文档）。
- 外部备份目录：`/Users/yage/ashare-backups/2026-08-18_pre-framework/`
  - `git_full.bundle`（完整 git 历史 + 全部 tags v1.0.0/v1.0.1/v1.1.0）
  - `project_full.tar.gz`（全量文件，含 .venv/.env，不含 .git）
  - `project_no_venv.tar.gz`（轻量文件，不含 .venv/.git，含 .env）
  - `requirements.txt`、`external_scripts/`、`launchd_plists/`、`ashare_logs/`、`data_extras/`
- 还原手册：`/Users/yage/ashare-backups/2026-08-18_pre-framework/RESTORE.md`（含 git 还原/文件还原/服务还原/验证步骤）。
- 注意事项：备份后不要再修改项目数据文件，保持备份点与仓库一致；新框架接入后如需回滚，先读 RESTORE.md。

## 6. 涨停过滤 + 交易日检查 + 日期一致性修复（2026-08-22）

### 6.1 问题描述
- **I32**: 8-20 报告预测日期是 8-19（数据滞后导致日期不一致）
- **I33**: 涨停股被推荐但买不进去（涨停过滤缺失）
- **I34**: 非交易日生成无意义报告（无交易日检查）

### 6.2 修复方案
| 变更ID | 文件 | 内容 | 提交 |
|--------|------|------|------|
| C25 | app/utils.py | 新增交易日检查函数（排除周末+法定假日） | f6f0edd |
| C26 | app/workflow.py | predict() 使用 collector 找到的交易日 | f6f0edd |
| C27 | app/feishu/bot.py | 非交易日发送复盘时提示用户 | f6f0edd |
| C28 | app/predict/candidate_pool.py | 候选池过滤涨停股（涨幅>=9.9%） | f6f0edd |

### 6.3 验证
- 飞书发送"复盘" → 显示"今天不是交易日"提示 ✅
- 8-21 报告预测日期与报告日期一致 ✅
- 涨停股贝瑞基因不再被推荐 ✅

### 6.4 回滚方式
```bash
git revert f6f0edd
```

## C25: 修复非交易日复盘失败 + 预测标的类型错误 (I36)
- **日期**: 2026-08-24
- **变更**: 
  - judge.py: 添加 targets 类型检查（isinstance dict）
  - workflow.py: 非交易日使用最近交易日继续执行
  - feishu bot.py: 修改非交易日警告消息
  - track.py: 添加 sell_date == pred_date 异常处理
- **影响**: 非交易日可正常生成预测，LLM 返回异常不再导致崩溃
- **回滚**: git revert <hash>

## C26: 修复 MCP 返回类型错误 (I37)
- **日期**: 2026-08-24
- **变更**: 
  - track.py: 添加 MCP 响应类型检查（isinstance dict）
  - workflow.py: 添加 MCP 响应类型检查
- **影响**: MCP 异常时不再导致结算失败
- **回滚**: git revert <hash>

## C27: 修复 MCP 缓存损坏导致预测失败 (I38)
- **日期**: 2026-08-24
- **变更**: 
  - 清空 mcp_cache.db 损坏缓存（1986 条）
  - app/predict/cache.py: MCPCache.get() 解析失败返回 None；CachedMcp.call() 归一化非 dict 响应
  - 补 cache.py log 定义
- **影响**: 复盘/预测恢复正常，推荐 3 只标的 + market_view
- **回滚**: git revert <hash>

## C28: 修复候选池数值字段类型崩溃 (I39)
- **日期**: 2026-08-25
- **变更**: 
  - candidate_pool.py: 新增 _safe_float()，_clean_rank_row 数值字段统一转 float，_is_valid 防御
- **影响**: MCP 返回字符串数值不再导致荐股失败
- **回滚**: git revert <hash>

## C29: 修复 LLM 研判偶发失败 (I40)
- **日期**: 2026-08-25
- **变更**: 
  - judge.py: max_tokens=8000 + medium effort 重试
  - llm.py: safe_json 第8层截断恢复
  - daily.py: LLM 失败降级（中性 market_view + 规则策略文案）
- **影响**: LLM 失败时平滑降级，不再显示"LLM研判失败"
- **回滚**: git revert <hash>
