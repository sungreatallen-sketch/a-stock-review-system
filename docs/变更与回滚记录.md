# 变更与回滚记录（CHANGELOG）

> 文档编号：CHG-001 ｜ 版本：v1.2 ｜ 用途：记录全部变更 + 提供可回滚索引（便于日后回滚）
> 关联：REQ-001 / ARCH-001 / ISSUE-001 / execution_rules.yaml

## 1. 版本基线
| 版本 | 日期 | 内容 | 提交 |
|---|---|---|---|
| v1.0 | 2026-08-12 | 需求/架构/规则工程化基线（问题1-5完成，待封装 tag） | c71dacb → 746ad14 |
| v1.1 | 2026-08-12 | 问题修复记录 + 需求升级 RQ-1.1~1.10 + R27 | 167c924 |
| v1.2 | 2026-08-12 | R21/R24/R25 实现 + 昨日复盘显眼化 + 因子库 R28 + 服务模板修复 | 2d86b88 / 575604b / d783cac / 746ad14 |

## 2. 变更记录（含回滚索引）
> 回滚方法：`git revert <hash>` 撤销代码；`git checkout <hash> -- <file>` 还原单文件；改代码后需重启对应服务。

| 变更ID | 内容 | 提交 | 回滚方法 |
|---|---|---|---|
| C01 | M1 基础：复盘采集/交叉验证/HTML/Web服务/飞书入口 | 985afbe | git revert 985afbe |
| C02 | M2 候选池+打分+量比过滤+回测+预测 | 7f15923 | git revert 7f15923 |
| C03 | M3 消息面+DeepSeek研判+模拟盘跟踪 | 2be8b06 | git revert 2be8b06 |
| C04 | M4 模拟盘自动化（自动结算+飞书结算+定时） | afc573d | git revert afc573d |
| C05 | 修复：飞书回复被系统代理拦截→直连+重试 | 81ea312 | git revert 81ea312 |
| C06 | 修复：消息去重+确认消息移后台+同日复用+NO_PROXY+.local | b2f7bdd | git revert b2f7bdd |
| C07 | 修复：回复去重键=(消息ID,类型) | 19d188e | git revert 19d188e |
| C08 | 统一复盘/预测为完整报告+HTML预测名称修复 | db227ee | git revert db227ee |
| C09 | 推荐跟踪：卡片昨日结算+HTML跟踪区块+ego结算兜底 | e599a15 | git revert e599a15 |
| C10 | 假设库（16条 hypothesis） | ee3cc01 | git revert ee3cc01 |
| C11 | H003/H001-16 独立回测（不改策略代码） | de04a57/fa5e852/4e43797 | git revert 对应提交 |
| C12 | 修复：手机打不开HTML→多链接策略 | 3bdfb0c | git revert 3bdfb0c |
| C13 | 修复：MCP自动发现端口+push2delay兜底+报告完整才复用+--force | de28108 | git revert de28108 |
| C14 | 修复：卡片昨日复盘显示条件 | 94379e1 | git revert 94379e1 |
| C15 | 评估双口径（开盘+收盘） | e13a9d2 | git revert e13a9d2 |
| C16 | 规则系统（execution_rules+RULES+合规自检+「规则」命令） | 526a3fe | git revert 526a3fe |
| C17 | 预测六步流程固化 R21-R26 + LLM截断回退 | 60edcd5 | git revert 60edcd5 |
| C18 | 问题1-2：工程文档(REQ/ARCH/ENG)+问题记录+规则v1.1 | c71dacb/167c924 | git revert 对应提交 |
| C19 | 问题3：R21(10日窗口)/R24(龙虎榜)/R25(买卖计划) | 2d86b88 | git revert 2d86b88 |
| C20 | 问题4：HTML昨日复盘上移+卡片顶部摘要 | 575604b | git revert 575604b；**并重启 com.ashare.server** |
| C21 | 问题5：因子库(46因子)+R28 | d783cac | git revert d783cac |
| C22 | 问题4再修复：报告服务缓存旧HTML模板→重启server+运维约定 | 746ad14 | 代码无需回滚（运维问题）；回滚方式=重启 server 后还原旧模板则改 html_report.py |
| C23 | 评估口径改为昨收买→今收卖（收盘-收盘），命中率按此计算；卡片/HTML 显示昨收/今收/收益 | (v1.4) | 回滚：git revert 本版本；重结算 .venv/bin/python run_cli.py track auto |
| C24 | 手机打不开HTML与VPN相关：lan_ip 改为优先默认路由物理网卡，排除 utun/tun 虚拟网卡 | (v1.4) | 回滚：git revert 本版本（还原 socket 取IP）；重启 bot/server |
| C25 | 报告图片直发飞书：复盘后自动生成手机宽度长图(3段)直接发到聊天，不依赖局域网/IP/VPN | (v1.4) | 回滚：git revert 本版本；重启 bot |
| C26 | 修复 C25 引入的 NameError('p')：_full_report_and_reply 引用未定义变量，导致整条复盘失败 | (v1.4) | 回滚：git revert 本版本；重启 bot |
| C27 | 图片上传改为直接 requests 调飞书 API；补充应用 im:resource 权限并发布新版本，图片直发功能打通 | (v1.4) | 回滚：git revert 本版本；权限如需撤销到控制台处理 |
| C28 | 图片卡片改为新消息直发聊天（requests 直调 message API，不再用回复），确保用户可见 | (v1.4) | 回滚：git revert 本版本；重启 bot |
| C29 | 后台数据积累文件 data/recommendation_history.json（全部已结算记录+日/累计/分股/按月分析），每次结算自动更新+CLI track history；HTML 跟踪表只显示最近一天（不累计展示） | (v1.1.0) | 回滚：git revert 本版本；重启 bot/server |
| C30 | 接入新框架前备份点：提交当前工作区(含RSI接入分析文档/最新数据/报告) ee8fd5c；外部备份 /Users/yage/ashare-backups/2026-08-18_pre-framework/（git bundle+全量/轻量tar包+requirements+脚本+launchd+日志） | ee8fd5c | 回滚：见 docs/变更与回滚记录.md 第5节 与 备份目录 RESTORE.md |
| C31 | RSI Framework 基础 Application 接入 Phase 1-9 完成：rsi_agent.py(AShareAgent) + rsi_app.py(Adapter入口) + rsi_tools.py(Tool适配) + rsi_memory.py(Memory适配) + rsi_evaluator.py(Evaluator适配) + bot.py(RSI开关) + config.yaml(rsi.enabled:false) + requirements.txt(RSI依赖) | (待提交) | 回滚：git revert 本提交；或将 config.yaml 的 rsi.enabled 改为 false 即回到 Legacy Flow |
| C32 | RSI Feedback Loop 闭环：rsi_evaluator v2(单次预测评估) + rsi_memory v2(Experience存储+去重) + rsi_feedback(settle->eval->memory桥接) + rsi_app集成触发 | (待提交) | 回滚：git revert 本提交；或 rsi.enabled:false 关闭 |
| C33 | RSI Insight Analyzer: rsi_insight.py(结构化Insight模型+统计分析引擎) + data/insights.json(持久化) + 样本门槛(≥3) + 去重(md5) | (待提交) | 回滚：git revert 本提交 |
| C34 | RSI Candidate Strategy: rsi_candidate.py(Insight→Candidate生成/回测/基准对比) + candidate/目录 + 数据泄漏控制(discovery/validation分离) + 样本门槛(MIN_TRADES=10) | (待提交) | 回滚：git revert 本提交; 删除candidate/ |
| C35 | RSI Walk-Forward Validation: rsi_validation.py(WalkForwardValidator+统计检验+鲁棒性+状态更新) + data/validation/ + Wilson CI/z-test | (待提交) | 回滚: git revert |
| C36 | RSI Shadow Mode: rsi_shadow.py(ShadowRunner+ShadowEvaluator+ShadowComparator) + shadow_predictions/shadow_results表 + prediction_mode隔离 + 数据源区分 | (待提交) | 回滚: git revert |
| C37 | safe_json 5层容错增强: 修复 DeepSeek reasoning_content 混入 content 导致研判解析失败 | 597eda6 | 回滚: git revert 597eda6 |
| C38 | R29合规自检修复: _attach_compliance pre变量bug + R29从空壳改为实际校验 + git pre-commit hook强制检查 | (待提交) | 回滚: git revert |
| C39 | 涨停股过滤修复: strategy.py过滤涨停+scoring.py扣分-5→-50+backtest.py跳过涨停 | (待提交) | 回滚: git revert |
| C40 | 涨停不可交易硬约束规范化: strategy.py(STRATEGY_VERSION=v1.1 + filter_reason=limit_up_untradable + self.filtered保留数据) + daily.py/workflow.py(strategy_version+filtered_out) + scoring.py回滚涨停扣分-50→-5 | (待提交) | 回滚: git revert |

## 3. 回滚操作手册
### 3.1 代码回滚
```bash
cd /Users/yage/Documents/我的预测系统
git log --oneline | head -30        # 查提交
git revert <hash>                   # 撤销某次变更（生成新的反向提交）
# 或仅还原某文件
git checkout <hash> -- app/predict/scoring.py
```
### 3.2 服务重启（改代码/模板后必须）
```bash
launchctl kickstart -k gui/$(id -u)/com.ashare.bot      # 机器人
launchctl kickstart -k gui/$(id -u)/com.ashare.server   # 报告服务（HTML模板变更必重启）
```
### 3.3 数据回滚（SQLite）
```bash
# 撤销某日错误结算
.venv/bin/python -c "
import sqlite3
from app.config import paths
c = sqlite3.connect(paths()['data'] / 'a_share.db')
c.execute('DELETE FROM prediction_results WHERE date=?', ('2026-08-12',))
c.commit()"
# 重新结算：.venv/bin/python run_cli.py track auto
```
### 3.4 规则回滚
- execution_rules.yaml 版本字段回退 + git revert 对应规则变更提交
- RULES.md 同步

## 4. 防复发约定（I24 教训）
- HTML 模板/前端变更后**必须重启 com.ashare.server**
- 任何代码变更后重启对应服务；用 curl 验证服务器实际输出
  `curl -s http://127.0.0.1:8787/report/<date> | grep 关键片段`

## 5. 备份点（接入新框架前，2026-08-18）
- 目的：接入 RSI Agent Framework 前的完整可回滚快照，防止新框架污染/损坏现有项目。
- Git 提交：`ee8fd5c`（备份点，基于 v1.1.0 之上，包含最新数据/报告/RSI 接入分析文档）。
- 外部备份目录：`/Users/yage/ashare-backups/2026-08-18_pre-framework/`
  - `git_full.bundle`（完整 git 历史 + 全部 tags v1.0.0/v1.0.1/v1.1.0）
  - `project_full.tar.gz`（全量文件，含 .venv/.env，不含 .git）
  - `project_no_venv.tar.gz`（轻量文件，不含 .venv/.git，含 .env）
  - `requirements.txt`、`external_scripts/`、`launchd_plists/`、`ashare_logs/`、`data_extras/`
- 还原手册：`/Users/yage/ashare-backups/2026-08-18_pre-framework/RESTORE.md`（含 git 还原/文件还原/服务还原/验证步骤）。
- 注意事项：备份后不要再修改项目数据文件，保持备份点与仓库一致；新框架接入后如需回滚，先读 RESTORE.md。

## 6. 涨停过滤 + 交易日检查 + 日期一致性修复（2026-08-22）

### 6.1 问题描述
- **I32**: 8-20 报告预测日期是 8-19（数据滞后导致日期不一致）
- **I33**: 涨停股被推荐但买不进去（涨停过滤缺失）
- **I34**: 非交易日生成无意义报告（无交易日检查）

### 6.2 修复方案
| 变更ID | 文件 | 内容 | 提交 |
|--------|------|------|------|
| C25 | app/utils.py | 新增交易日检查函数（排除周末+法定假日） | f6f0edd |
| C26 | app/workflow.py | predict() 使用 collector 找到的交易日 | f6f0edd |
| C27 | app/feishu/bot.py | 非交易日发送复盘时提示用户 | f6f0edd |
| C28 | app/predict/candidate_pool.py | 候选池过滤涨停股（涨幅>=9.9%） | f6f0edd |

### 6.3 验证
- 飞书发送"复盘" → 显示"今天不是交易日"提示 ✅
- 8-21 报告预测日期与报告日期一致 ✅
- 涨停股贝瑞基因不再被推荐 ✅

### 6.4 回滚方式
```bash
git revert f6f0edd
```

## C25: 修复非交易日复盘失败 + 预测标的类型错误 (I36)
- **日期**: 2026-08-24
- **变更**: 
  - judge.py: 添加 targets 类型检查（isinstance dict）
  - workflow.py: 非交易日使用最近交易日继续执行
  - feishu bot.py: 修改非交易日警告消息
  - track.py: 添加 sell_date == pred_date 异常处理
- **影响**: 非交易日可正常生成预测，LLM 返回异常不再导致崩溃
- **回滚**: git revert <hash>

## C26: 修复 MCP 返回类型错误 (I37)
- **日期**: 2026-08-24
- **变更**: 
  - track.py: 添加 MCP 响应类型检查（isinstance dict）
  - workflow.py: 添加 MCP 响应类型检查
- **影响**: MCP 异常时不再导致结算失败
- **回滚**: git revert <hash>

## C27: 修复 MCP 缓存损坏导致预测失败 (I38)
- **日期**: 2026-08-24
- **变更**: 
  - 清空 mcp_cache.db 损坏缓存（1986 条）
  - app/predict/cache.py: MCPCache.get() 解析失败返回 None；CachedMcp.call() 归一化非 dict 响应
  - 补 cache.py log 定义
- **影响**: 复盘/预测恢复正常，推荐 3 只标的 + market_view
- **回滚**: git revert <hash>

## C28: 修复候选池数值字段类型崩溃 (I39)
- **日期**: 2026-08-25
- **变更**: 
  - candidate_pool.py: 新增 _safe_float()，_clean_rank_row 数值字段统一转 float，_is_valid 防御
- **影响**: MCP 返回字符串数值不再导致荐股失败
- **回滚**: git revert <hash>

## C29: 修复 LLM 研判偶发失败 (I40)
- **日期**: 2026-08-25
- **变更**: 
  - judge.py: max_tokens=8000 + medium effort 重试
  - llm.py: safe_json 第8层截断恢复
  - daily.py: LLM 失败降级（中性 market_view + 规则策略文案）
- **影响**: LLM 失败时平滑降级，不再显示"LLM研判失败"
- **回滚**: git revert <hash>

## C30: 修复 MCP K线查询漏 end_date (I41)
- **日期**: 2026-08-26
- **变更**: track.py settle_pending、backtest.py _index_5d_returns/_index_benchmark 改用 end_date+limit
- **影响**: 结算不再误判"无次日数据"
- **回滚**: git revert <hash>

## C31: 实现 R11 预测锁定复用，防预测覆盖 (I42)
- **日期**: 2026-08-26
- **变更**: Tracker.get_prediction() + workflow 生成前复用检查
- **影响**: 用户看到的预测=结算的预测，不被意外覆盖
- **回滚**: git revert <hash>

## C32: MCP 缓存加 TTL + R11 下沉到实际交易日 (I43)
- **日期**: 2026-08-26
- **变更**: cache.py TTL（当天有效）；daily.py predict R11 基于实际 t 复用；恢复 8-25 预测并重结算
- **影响**: 交易日不再误判，预测不被覆盖
- **回滚**: git revert <hash>

## C33: 新增自动化复盘（18:00定时 + 开机补执行 + 插电检测）
- **日期**: 2026-08-26
- **变更**:
  - scripts/send_review.py: 自动复盘后发飞书（卡片+图片）
  - ashare-scripts/run_review.sh: 插电检测→已复盘检测→执行复盘→发飞书
  - ~/Library/LaunchAgents/com.ashare.review.plist: RunAtLoad(开机补执行) + StartInterval 1800s(兜底) + StartCalendarInterval 18:00工作日
- **机制**:
  - 插电(AC Power)才执行；未插电跳过（等下次触发/开机补执行）
  - 今天已复盘(reports/<today>.json完整)则跳过，避免重复
  - data/last_review_sent_<date>.flag 去重，避免重复发飞书
- **回滚**: 卸载 launchctl unload com.ashare.review

## C34: 修复指数抓取失败 + 自动复盘加交易日检查 (I44)
- **日期**: 2026-08-26
- **变更**: ego_scraper index 加 push2delay 兜底 + parse_index 防御 + collector _try_load + run_review.sh 交易日检查
- **影响**: 自动复盘报告数据完整，周末/节假日不自动触发
- **回滚**: git revert <hash>

## C35: 修复自动复盘凌晨误触发 (I45)
- **日期**: 2026-08-27
- **变更**: 新增 scripts/auto_review.py（最近已收盘交易日判断）；run_review.sh 重写；删除错误 8-27 报告
- **影响**: 凌晨/盘前不误触发，晚上18:00正常复盘当天
- **回滚**: git revert <hash>

## C36: 缓存 TTL 改为15分钟，修复结算漏当日 (I46)
- **日期**: 2026-08-27
- **变更**: cache.py MCP_TTL_SECONDS=900（原"当天有效"）；重新结算 8-26
- **影响**: 凌晨缓存不会整天命中，结算能拿到当日数据
- **回滚**: git revert <hash>

## C37: "昨日推荐标的"显示前天标的修复 + WorkBuddy MCP 工具屏蔽 (I47)
- **日期**: 2026-08-28
- **变更**:
  - app/workflow.py `_build_tracking`: 增加 `latest_prediction` 字段（含未结算预测）
  - app/feishu/bot.py: 飞书卡片"昨日推荐标的"用 `latest_prediction` 替代 `stats.recent`
  - scripts/send_review.py: 自动复盘脚本同步修改
  - app/html_report.py: HTML 报告同步修改
- **影响**: 无论结算是否成功，"昨日推荐标的"始终显示昨天的预测；未结算时显示"待结算"
- **回滚**: git revert <hash>

## C38: WorkBuddy MCP tongzhou-fin-research connector-proxy 屏蔽 fin_data 工具 (I48)
- **日期**: 2026-08-28
- **现状**: WorkBuddy catalog 机制拒绝 `fin_data` 系列工具（`missing_ui_resourceUri`），connector-proxy 无法路由调用
- **影响**: 预测模块无法获取 K 线数据 → 预测失败 → 结算失败
- **Workaround**: 无（需 WorkBuddy 框架侧修复，或绕过 connector-proxy 直连 MCP 服务器）
- **待办**: 用户确认 WorkBuddy 侧可调用后，研究直连 MCP 服务器方案

## C39: ego browser 兜底支持收盘价，修复 MCP 不可用时结算失败 (I48)
- **日期**: 2026-08-28
- **变更**:
  - app/predict/alt_data.py: `EgoOpenPrices.fetch_with_close()` 返回 (opens, closes) 二元组
  - app/predict/track.py: `settle_pending` ego 兜底同时取收盘价
- **影响**: MCP 不可用时结算仍能获取收盘价计算收益
- **回滚**: git revert <hash>

## C40: ego browser K线域名修复 + 结算兜底条件修正 (I48)
- **日期**: 2026-08-28
- **变更**:
  - app/predict/alt_data.py: `_kline_url` 域名从 `push2delay` 改为 `push2his`（push2delay 个股K线为空）
  - app/predict/alt_data.py: 新增 `fetch_with_close()` 同时返回开盘价+收盘价
  - app/predict/track.py: `settle_pending` 兜底条件从 `not opens and mcp_failed` 改为 `not opens`（缓存层吞掉错误导致 mcp_failed 不触发）
- **验证**: 8/27 预测 3 只标的已成功结算（敦煌种业+10.05%、天山生物-7.75%、长进光子-6.28%）
- **回滚**: git revert <hash>

## C41: 预测模块 K 线查询 ego 兜底（MCP 不可用时仍可出预测）
- **日期**: 2026-08-28
- **变更**:
  - app/predict/daily.py `_kline_lookup_factory`: MCP 失败时切 ego browser（push2his），返回格式转换为 MCP 兼容
  - app/predict/backtest.py `trading_days`: MCP 交易日历为空时切 ego browser 兜底
- **影响**: WorkBuddy MCP 被屏蔽时，预测仍可通过 ego browser 获取 K 线数据
- **回滚**: git revert <hash>

## C42: settle_pending 只结算昨日预测 + MCP snapshot 兜底
- **日期**: 2026-08-28
- **变更**:
  - app/predict/track.py:
    - settle_pending 只找昨天及之前的未结算预测（今天的预测今天不能结算）
    - 新增 `_find_settleable_prediction(today)` 方法
    - MCP K线失败时用 `get_latest_snapshot` 获取收盘价（MCP 可用工具）
- **影响**: 结算逻辑正确，不再尝试结算今天的预测；收盘价获取增加 MCP 兜底
- **回滚**: git revert <hash>

## C43: 同花顺金融数据 API 接入（替代 MCP 行情/K线数据源）

- **日期**: 2026-08-28
- **变更**: 
  - 新增 `app/ths_client.py`：同花顺 REST API 客户端（交易日历、快照、K 线、搜索、异动、龙虎榜、财务）
  - 修改 `config/config.yaml`：新增 `ths` 配置段（api_key + base_url）
  - 修改 `app/collector.py`：`_collect_index()` THS 指数快照优先 → 东财 ego → 通达信 MCP 兜底
  - 修改 `app/predict/track.py`：`settle_pending()` THS 交易日历优先 + THS snapshot 兜底收盘价
  - 修改 `app/predict/backtest.py`：`_kline()` THS K 线优先 → MCP 兜底
  - 修改 `app/predict/daily.py`：`_kline_lookup_factory()` THS K 线优先 → MCP → ego 兜底
- **影响**: 
  - 指数数据源从「东财 ego + 通达信 MCP 验证」改为「同花顺 API 为主」（验证源缺失，仅同花顺单源）
  - K 线数据不再依赖被 WorkBuddy catalog 屏蔽的 `get_kline_series`
  - MCP 仍用于新闻/公告/涨停池（doc_search 正常，情绪数据正常）
- **验证**: 8/28 复盘成功生成，指数/情绪/板块/预测全部正常，已发飞书
- **回滚**: git revert <hash>

---

### CHG-043：修复昨日推荐标的复盘显示与结算结果
- **日期**：2026-08-28
- **类型**：Bug修复
- **变更内容**：
  - `app/workflow.py` — `_build_tracking()` 增加跳过当日预测逻辑 + 主动查询已结算结果
- **关联问题**：ISSUE-027
- **回滚方式**：`git checkout HEAD~1 app/workflow.py`
- **测试结果**：✅ 复盘报告正确显示昨日标的及结算收益

---

### CHG-044：三层防御体系（报告校验器 + 回归测试 + 结构化日志）
- **日期**：2026-08-28
- **类型**：架构增强
- **变更内容**：
  - 新增 `app/report_validator.py` — 报告校验器（V01-V08 规则）
  - 新增 `app/review_logger.py` — 结构化复盘日志
  - 新增 `tests/test_regression.py` — 18 个回归测试用例
  - 修改 `app/workflow.py` — 集成校验器 + 结构化日志
  - 修改 `app/feishu/bot.py` — 集成校验器
- **关联问题**：ISSUE-027（反复出现的日期/结算错误）
- **回滚方式**：删除新增文件 + `git checkout HEAD~1`
- **测试结果**：✅ 18/18 回归测试通过，复盘运行 validation=PASS

---

### CHG-045：16:00 终版推送与数据源健康门禁
- **日期**：2026-09-02
- **类型**：调度修复
- **变更内容**：
  - `scripts/auto_review.py` 新增收盘终版判断、16:00 门禁、终版 flag 绑定、MCP/THS 健康检查与同日告警去重。
  - `scripts/check_data_sources.py` 输出结构化健康状态并解包 MCP 错误。
  - `scripts/send_feishu_alert.py` 修复 JSON 换行转义。
  - `scripts/run_scheduled_review.sh` 使用完整时间判断并只做插电门禁。
  - `execution_rules.yaml` 升级 v1.6，R15/R32 强化。
- **关联问题**：ISSUE-030 / ISSUE-032
- **测试结果**：24/24 回归通过；launchd RunAtLoad exit 0；9/2 终版报告已发飞书。
- **回滚方式**：`git revert` 本提交；如需回退 launchd，恢复旧 Plist 后执行 `launchctl bootout` + `bootstrap`。

### CHG-046：真实可执行结算窗口 T+1 收盘买 → T+2 收盘卖
- **日期**：2026-09-02
- **类型**：核心口径修复
- **变更内容**：
  - `app/predict/track.py` 结算改为 T+1/T+2 双收盘价；卖出日未到不结算；任一标的价格缺失不整体结算。
  - 持久化 `buy_date/sell_date/reference_price`；导出与统计包含真实执行日期。
  - `app/workflow.py` 修复结算详情字段映射。
  - 新增 `scripts/recalculate_settlements.py`，支持 dry-run/apply，apply 前自动备份数据库。
- **关联问题**：ISSUE-031
- **数据修正**：重算 2026-08-11 至 2026-08-31 共 12 个交易日、36 笔；数据库备份见 `data/a_share_backup_before_recalc_20260902_184029.db`。
- **测试结果**：24/24 回归通过。
- **回滚方式**：`git revert` 代码；数据库用上述备份恢复。注意：恢复数据库会同时回退历史收益口径。

### CHG-047：MCP 代理去 VPN 污染 + launchd ASCII 入口
- **日期**：2026-09-02
- **类型**：数据源与部署修复
- **变更内容**：
  - `app/mcp_client.py` 不再回落陈旧本地端口，并强制 MCP HTTP `trust_env=False`。
  - 健康检查展示真实底层异常。
  - launchd 实际入口改为 `/Users/yage/ashare-scripts/run_ashare_review_v15.sh`，项目路径通过 `/Users/yage/ashare-project` 符号链接。
- **关联问题**：ISSUE-032
- **测试结果**：launchd 服务 RunAtLoad exit 0；THS 健康检查 OK；MCP 失败时可产生结构化告警。
- **回滚方式**：`git revert app/mcp_client.py` 与健康检查脚本；launchd 恢复旧 Plist。

### CHG-048：通达信官方MCP OAuth直连与统一兜底工厂
- **日期**：2026-09-02
- **类型**：数据源架构增强
- **变更内容**：
  - 新增 `app/tdx_mcp_client.py`，直连通达信官方streamable-http MCP。
  - 新增 `ResilientMcpClient`：WorkBuddy优先 → TDX直连；同舟K线输出兼容`data.points`。
  - `workflow/collector/sweep`统一走`create_resilient_client()`。
  - 健康检查新增`tdx_direct`；自动复盘可接受TDX直连作为MCP可用状态。
  - `execution_rules.yaml`升级v1.7/R33；`data/tdx_oauth.json`加入`.gitignore`。
- **关联问题**：ISSUE-033
- **安全**：token文件权限600、不打印、不提交；所有MCP直连请求禁用系统代理。
- **测试结果**：通达信直连19工具；上证指数K线3点正确；26/26 pytest通过。
- **回滚方式**：`git revert`本提交；或将`create_resilient_client()`调用点恢复为WorkBuddy `McpClient`。OAuth文件删除后需重新`scripts/tdx_oauth_login.py login --open`。

### CHG-049：同舟/Wind官方MCP直连与多源健康门禁
- **日期**：2026-09-02
- **类型**：数据源架构增强
- **变更内容**：
  - 新增通用OAuth/static bearer `DirectMcpClient`。
  - 新增同舟OAuth动态注册、浏览器授权、refresh/test脚本。
  - `ResilientMcpClient`支持`tdx-connector_*`、`tongzhou-fin-research_*`、`wind-finance_*`前缀；缺失工具不伪造。
  - 健康检查与16:00门禁新增`tongzhou_direct`、`wind_direct`。
  - `execution_rules.yaml`升级v1.8/R34。
- **关联问题**：ISSUE-034
- **安全**：OAuth文件600并gitignore；不打印token；不解密WorkBuddy凭据；直连禁用系统代理。
- **测试结果**：同舟16工具、token刷新成功；统一直连真实调用成功；28/28 pytest通过。
- **当前限制**：Wind缺`WIND_API_KEY`或OAuth凭据，状态FAIL；等待用户补充。
- **回滚方式**：`git revert`本提交；或从`create_resilient_client()`移除同舟/Wind客户端。同舟token删除后重新运行`scripts/direct_mcp_oauth.py login tongzhou --open`。
