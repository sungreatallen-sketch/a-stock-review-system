# 工程记忆（PROJECT MEMORY）—— 大模型接入本项目的必读文件

> 版本：v1.1.0 ｜ 更新：2026-08-14
> **用途：任何新版本开发或 bug 修复前，必须先阅读本文件**，再按需阅读引用的权威文档。
> 本文件是"项目当前状态速览"，权威细节在下方引用文档中。

## 0. 一句话
A股收盘复盘 + 次日标的预测 Agent：飞书手动触发 → ego浏览器/MCP 采集交叉验证 → 规则六步预测 → 手机可打开的 HTML 报告 → 昨日标的次日评估与命中率。

## 1. 必读顺序（按此读）
1. 本文档（docs/工程记忆.md）
2. execution_rules.yaml（执行规则 R01–R30，唯一事实源）
3. docs/需求规格说明书.md（REQ-001，原始需求基线）
4. docs/架构设计方案.md（ARCH-001，定稿架构）
5. docs/变更与回滚记录.md（CHG-001，变更回滚索引）
6. docs/问题与修复记录.md（ISSUE-001，I01–I26）
7. factors/README.md + investment_factors.yaml（因子库，隔离未接入）

## 2. 当前版本状态
- 执行规则：**v1.4，30 条**（R01–R30）
- 工程基线：REQ-001 / ARCH-001 / CHG-001 / ISSUE-001 全部就绪
- 已封装：**v1.0.0**（git tag）
- 合规自检：每次复盘/预测输出附自检，红线违规显式上报

## 3. 总体架构
```
飞书(手动触发) → 编排层 workflow.run_review
  → 数据采集：ego浏览器(东财 push2delay 优先) + MCP(通达信/同舟/Wind 兜底链)
  → 交叉验证(validator 双源容差)
  → 存储(SQLite + reports/*.json)
  → 预测六步：S1板块(10日) → S2候选池 → S3打分 → S4消息面(含龙虎榜) → S5 LLM研判(3只+买卖计划) → S6回测命中率
  → HTML报告(手机自适应) → 本地Web服务 :8787
  → 规则预读(R30) + 合规自检(R20)
```

## 4. 关键机制（改了容易踩坑，务必先看）
| 机制 | 要点 | 引用 |
|---|---|---|
| 数据源 | ego优先；MCP自动发现端口/token（WorkBuddy重启端口会变）；MCP连不上必须上报用户(R03) | R01-R03/R27 |
| 网页抓取 | 东财 push2delay 备用域名（主域名会限流） | R01/I06 |
| 评估口径 | **昨收买→今收卖（收盘-收盘）**，命中率按此计算 | R10/R14/I25 |
| 同日复用 | 同日预测锁定复用；报告完整(板块+预测+跟踪)才复用，否则强制重生成 | R11/R16 |
| 去重 | 消息级+回复级（键=消息ID+类型） | R17 |
| 手机访问 | 卡片给 IP 主链接+.local 备用+当前IP；**IP 取默认路由物理网卡，排除 utun/tun** | R09/I26 |
| 代理 | 飞书/DeepSeek 直连（NO_PROXY+trust_env=False），不走 clash 系统代理 | I02 |
| 变更日志 | 任何修复/更新必须记 CHG+ISSUE（R29），无日志=未完成 | R29 |
| 运行预读 | 运行前必须预读 execution_rules.yaml，失败禁止运行（R30） | R30 |
| 因子 | 投资经验因子隔离于 factors/，未验证未批准不得接入（R28） | R28 |

## 5. 更新修复历史（截至 v1.1.0）
- 里程碑：M1 复盘采集/HTML/飞书 → M2 候选池+回测 → M3 消息面+LLM+模拟盘 → M4 模拟盘自动化
- 问题修复：I01–I26（详见 ISSUE-001），变更 C01–C24（详见 CHG-001）
- 重点教训：
  - 报告服务常驻进程缓存旧 HTML 模板 → **改模板必须重启 server**（I24）
  - MCP 端口漂移 → 自动发现（I01）
  - 同日预测被 LLM 覆盖 → 锁定复用（I09）
  - 评估口径 开盘→收盘（用户决策，I25）
  - VPN 开时 IP 取错 → 取物理网卡（I26）
  - 报告图片直发飞书（I27-I30）：补 im:resource 权限，图片以新消息直发聊天，彻底绕开 VPN/IP 打不开问题
  - 后台数据积累（I31）：data/recommendation_history.json 积累全部已结算记录+分析；HTML 只显示最近一天

## 6. 运维速查
```bash
# 重启服务（改代码/模板后必须）
launchctl kickstart -k gui/$(id -u)/com.ashare.bot
launchctl kickstart -k gui/$(id -u)/com.ashare.server   # HTML模板变更必重启
# 验证服务端实际输出（防模板缓存）
curl -s http://127.0.0.1:8787/report/<date> | grep 关键片段
# 回滚
git revert <hash>   # 见 CHG-001
# 重结算
.venv/bin/python run_cli.py track auto
# 完整复盘(强制重生成)
.venv/bin/python run_cli.py review --force
```


## 7b. RSI Framework 接入状态（v1.1+）
- **状态**：基础 Application 接入完成（Phase 1-9），RSI 自进化未启用
- **开关**：`config.yaml` → `rsi.enabled: false`（默认 Legacy Flow）
- **新增文件**：
  - `app/rsi_app.py` — Application Adapter 入口（handle_command）
  - `app/rsi_agent.py` — AShareAgent（EnhancedAgent 实现，委托 workflow）
  - `app/rsi_tools.py` — Tool 适配（CollectDataTool / McpQueryTool）
  - `app/rsi_memory.py` — Memory 适配（从 recommendation_history.json 只读加载）
  - `app/rsi_evaluator.py` — Evaluator 适配（包装 Tracker.stats）
- **修改文件**：
  - `app/feishu/bot.py` — `_full_report_and_reply()` 增加 rsi.enabled 开关
  - `config/config.yaml` — 增加 `rsi.enabled` 配置项
  - `requirements.txt` — 新增 RSI Framework 依赖（langgraph 等 16 个包）
- **RSI Framework 路径**：`/Users/yage/Claude code/vibecoding1/rsi-agent-framework`（.pth 接入，未修改 Framework 代码）
- **回滚方式**：`rsi.enabled: false` 即回到 Legacy Flow；或 `git revert` 撤销全部代码
- **未实现**：RSI 自我进化 / CandidateStrategy / Promotion / Rollback / 自动策略修改
- **Phase 5 Walk-Forward Validation**（已完成）：
  - WalkForwardValidator: 3窗口滚动验证
  - 统计检验: Wilson CI + z-test (α=0.10)
  - 鲁棒性: 一致性/退化检测
  - 状态更新: candidate→validated/rejected/insufficient_data
  - 不自动 Promotion
- **Phase 4 Candidate Strategy**（已完成）：
  - AShareCandidateGenerator: Insight→Candidate（6种规则）
  - BacktestRunner: 复用现有Backtest.run()，注入自定义Strategy
  - BenchmarkComparator: Candidate vs Production 对比
  - 数据泄漏控制: discovery/validation 60/40分离
  - 样本门槛: MIN_TRADES=10
  - Candidate 状态: candidate/backtested/validated/insufficient_data/rejected
  - 不修改Production，不自动上线
- **Phase 3 Insight**（已完成）：
  - AShareInsightAnalyzer：统计分析（非 LLM），6 维度分组
  - 样本门槛 MIN_SAMPLE=3，不足标记 insufficient_data
  - 持久化 data/insights.json，去重（insight_id=md5）
- **Phase 2 反馈闭环**（已完成）：
  - settle 后自动触发 Evaluation → Memory（rsi_feedback.py）
  - Evaluator 评价单次预测表现（非汇总统计）
  - Memory 存储 Experience（prediction+evaluation+actual_result+market_context）
  - 去重机制：prediction_id 唯一键
  - 历史数据可从 SQLite 重建

## 6b. 已知修复记录
- I32: safe_json 5层容错增强（DeepSeek reasoning 混入 content 导致研判失败）— 597eda6

## 7. 待办/未来（未纳入 v1.0）
- 因子库 46 个因子隔离待成熟（R28 批准后可接入）
- 后台数据积累文件：data/recommendation_history.json（自动更新，前端不展示累计）
- 报告图片直发：已上线（v1.0.1，C25-C28/I27-I30）
- "历史 N 日滚动回测"作为预测流程显式步骤（R26 部分实现）
- 公网隧道（cloudflared）网络恢复后启用（I17）
- 模拟盘验证期：每日复盘/预测 → 次日自动结算 → 周度命中率评估
- 更多假设独立回测（H005/H008/H010/H012/H014/H016 缺数据待补）

## 6b. 已知修复记录（续）
- I34: safe_json 7层容错增强（单引号/混合内容导致研判失败）— 2026-08-24

## 6b. 已知修复记录（续）
- I36: 非交易日复盘失败 + judge targets 类型错误 — 2026-08-24

## 6c. 关键教训：MCP 查询区间语义 + 预测锁定复用（重要，勿踩坑）
> 本小节为【高频坑】，任何修改 K 线查询/预测流程前必须先读。

### 坑1：MCP get_kline_series 的 start_date+end_date 是左闭右开
- **现象**：`{"start_date":"2026-08-25","end_date":"2026-08-26"}` 返回 `['2026-08-25']`，**漏掉 end_date 当天**
- **后果**：settle_pending 结算时误判"次日无交易日数据"，8-25 预测未在 8-26 结算
- **规则**：**所有 K 线查询一律用 `{"end_date": X, "limit": N}`，禁止同时传 start_date+end_date**；需要起始过滤时在 Python 侧做 `p["time"] >= start`
- **已修复文件**：track.py settle_pending、backtest.py _index_5d_returns / _index_benchmark

### 坑2：R11 同日预测锁定复用必须真实现（防止预测被覆盖）
- **现象**：数据源滞后时最新交易日误判为前一天，predict() 重新生成并 upsert **覆盖了用户已看到的预测**
- **后果**：用户看到的预测（如和元生物）与实际结算的预测（如康泰生物）不一致
- **规则**：**生成预测前必须先 `Tracker.get_prediction(trade_date)` 检查，已有则复用，绝不覆盖**
- **已修复文件**：workflow.py run_review（R11 实现）、track.py 新增 get_prediction()

### 相关修复记录：I41（MCP区间） / I42（预测覆盖），变更 C30/C31

## 6d. 关键教训：MCP 缓存必须有 TTL（高频坑，勿踩）
> 修改缓存/交易日逻辑前必读。

- **现象**：某时刻缓存的 K 线数据（不含当日）永久命中，导致 trading_days 误判最新交易日为前一天，进而覆盖用户已看到的预测、结算错误标的
- **规则**：**MCPCache 缓存当天有效，跨天自动过期重拉**（cache.py 已实现 TTL）
- **规则**：**R11 复用检查必须基于 trading_days 实际算出的 t，不能用外部传入的 target_date**（daily.py 已实现）
- **恢复操作**：预测被覆盖时，可从 reports/<date>.json 的 prediction 恢复 predictions 表

## 6e. 关键教训：指数抓取必须走 push2delay 兜底（高频坑）
- **现象**：push2.eastmoney.com 主域名易被限流返回空，指数抓不到 → 报告大盘数据 null
- **规则**：**所有 push2 请求必须用 _with_fallback（push2delay 优先）**；collector 对 raw 项用 _try_load 解析
- **规则**：自动复盘脚本必须加交易日检查，周末/节假日跳过

## 6f. 关键教训：自动复盘必须用"最近已收盘交易日"
- **现象**：凌晨/盘前触发自动复盘，用昨天数据生成"今天"报告，半夜发飞书
- **规则**：**目标交易日 = 最近已收盘交易日**（今天过15:30且交易日→今天；否则从昨天往前找）
- **实现**：scripts/auto_review.py（勿用 get_latest_trading_day 直接判断，它含今天）

## 6g. 关键教训：MCP 缓存 TTL 必须短（勿用"当天有效"）
- **现象**：凌晨缓存了不含当日的 K 线数据，"当天有效"TTL 导致整天命中旧数据，结算漏掉当天
- **规则**：**MCP 缓存 TTL = 15 分钟**（MCP_TTL_SECONDS=900）；不能用"当天有效"，因为凌晨可能缓存"不含当日"的半截数据

## 8. 2026-08-28 更新记录

### 8.1 已修复问题
- **I47/C37**: "昨日推荐标的"显示前天标的 → 改用 `latest_prediction`（含未结算预测）
  - 修改文件：workflow.py, bot.py, send_review.py, html_report.py
  - 验证：8/28 报告 `tracking.latest_prediction.date=2026-08-27`

### 8.2 待解决问题
- **I48/C38**: WorkBuddy MCP connector-proxy 屏蔽 `tongzhou-fin-research` 的 `fin_data` 工具
  - 现象：connector-proxy 返回 "Tool not found"，但 WorkBuddy 内部测试正常
  - 根因：catalog 机制要求 `ui_resourceUri`，`fin_data` 工具缺少该元数据
  - 影响：预测失败、结算失败
  - 状态：待 WorkBuddy 侧修复或绕过 connector-proxy 直连

### 8.3 关键文件清单
| 文件 | 作用 |
|---|---|
| `app/workflow.py` | `_build_tracking()` 组装跟踪数据（含 latest_prediction）|
| `app/feishu/bot.py` | 飞书卡片"昨日推荐标的"显示逻辑 |
| `scripts/send_review.py` | 自动复盘发飞书 |
| `app/html_report.py` | HTML 报告"昨日推荐标的"显示逻辑 |
| `app/predict/track.py` | Tracker 结算逻辑（settle_pending）|
| `app/mcp_client.py` | MCP 调用客户端 |
| `scripts/auto_review.py` | 自动复盘核心逻辑（交易日判断）|

### 8.4 今日验证清单
- [ ] 飞书卡片"昨日推荐标的"显示 8/27（敦煌种业/天山生物/长进光子）
- [ ] 未结算时显示"待结算"
- [ ] WorkBuddy MCP 问题排查

## 8b. 同花顺 API 接入（2026-08-28）
- **状态**：已接入，作为行情/K线主数据源
- **配置**：`config.yaml` → `ths.api_key` / `ths.base_url`
- **新增文件**：`app/ths_client.py`（THSClient 单例）
- **修改文件**：collector.py / track.py / backtest.py / daily.py（THS 优先 → MCP → ego 兜底）
- **数据源分工**：
  - 同花顺 API：指数快照、K 线、交易日历、收盘价、财务、龙虎榜
  - MCP（保留）：新闻/公告（doc_search）、涨停池/情绪（screen_stocks）
  - ego browser（保留）：东财板块数据、涨停池、MCP 不可用时兜底
- **未替代 MCP 的部分**：新闻/公告（doc_search 工具正常，不受 catalog 屏蔽）
