# 项目问题与修复记录（执行日志）

> 文档编号：ISSUE-001 ｜ 版本：v1.1
> 用途：完整记录系统执行过程中遇到的问题、根因、修复与关联规则；
>       作为"经验教训"工程化，防止同类问题复发，并驱动需求规则升级。

| ID | 日期 | 问题 | 根因 | 修复 | 关联规则 |
|----|------|------|------|------|----------|
| I01 | 08-12 | MCP 全部失联（预测失败） | WorkBuddy 重启后代理端口 55056→56555，客户端写死 5505x 找不到 | 客户端自动发现最新端口+token（扫描日志+验证监听） | R03 |
| I02 | 08-11 | 飞书回复偶发失败 | requests 走 clash 系统代理，对 feishu 路由不稳定 | NO_PROXY+trust_env=False 直连+重试 | R06 |
| I03 | 08-11 | 复盘输出两份相同结果 | ①确认消息同步发送阻塞事件处理→飞书长连接重投事件；②同日报告未复用 | 消息级去重+确认消息移后台线程+同日报告复用 | R17 |
| I04 | 08-11 | 手机点开 HTML 打不开 | ①局域网 IP 漂移(1.x↔3.x)；②.local mDNS 跨设备常失效 | 卡片多链接（IP 主按钮+.local 备用+当前 IP 提示） | R09 |
| I05 | 08-11 | 只收到"收到！"无结果卡片 | 去重键误用为消息 ID，确认消息把结果卡片当重复跳过 | 去重键=(消息ID,回复类型) | R17 |
| I06 | 08-12 | 板块排行空数据 | 东财 push2 clist 被限流（大量抓取触发） | 备用域名 push2delay+多候选 URL 自动切换 | R01/R08 |
| I07 | 08-12 | 同日坏报告被复用 | 复用逻辑不检查报告完整性 | 报告完整(板块+预测+跟踪)才复用，否则强制重生成，CLI 加 --force | R16 |
| I08 | 08-12 | 昨日标的复盘不显示 | 卡片逻辑只在"本次刚结算"时显示，已结算记录被跳过 | 有已结算记录即显示昨日复盘 | R12/R13 |
| I09 | 08-12 | 昨日标的与今日结算对不上 | 同日预测被 LLM 重生成覆盖（标的漂移 北化→云路） | 同日预测锁定复用（用户看到的=结算的） | R11 |
| I10 | 08-12 | 复盘只看开盘不看收盘 | 结算仅存次日开盘价 | 结算增加收盘价双口径（sell_close/ret_close），卡片+HTML 展示 | R14 |
| I11 | 08-12 | LLM 输出截断致预测 0 只 | judge 解析失败且无回退 | LLM 失败自动回退规则候选；max_tokens 3000→5000 | R10 |
| I12 | 08-11 | launchd 服务启动失败(EX_CONFIG/127) | launchd 无法处理中文路径（ProgramArguments/日志路径） | 脚本与日志改英文路径（ashare-scripts / ashare-logs），脚本内 cd 中文项目路径 | — |
| I13 | 08-11 | 后台进程被会话回收 | nohup 在 exec 会话结束后不持久 | 全部改 launchd 常驻（bot/server/settle/caffeinate） | — |
| I14 | 08-12 | ego 抓取返回空 | ego-browser cliLog 输出在 stderr，只解析了 stdout | stdout+stderr 均解析 | R01 |
| I15 | 08-12 | ego 脚本报 BASE 未定义 | Node 模板引用 BASE 未注入 | 占位符替换注入 BASE | R01 |
| I16 | 08-12 | 批量抓取触发东财限流 | H3 回测大量拉 K 线 | 低频抓取+断点续传+优先复用已有数据；后续走 push2delay | R01 |
| I17 | 08-12 | 公网隧道无法建立 | 国内网络访问 Cloudflare/GitHub 不通 | 暂缓；代码已预留隧道 URL 读取（report_link.public） | R09 |
| I18 | 08-12 | 结算 stats 字段列错位 | 表加列后 SELECT 列序与映射不一致 | 修正字段映射并本地验证 | R14 |
| I19 | 08-12 | 合规自检暴露 R21/R24/R25 缺口 | 规则系统发现代码未实现项 | 列入工程待办（问题3处理） | R21/R24/R25 |
| I20 | 08-12 | 板块/预测/跟踪齐全性无法保证 | 无强制校验 | 合规自检接入 workflow，每次输出附自检 | R20 |
| I21 | 08-12 | R21 板块窗口 5日 vs 7-10日口径不符 | 原用同舟5日资金流窗口 | 改为东财行业板块10日涨幅（ego优先，同舟20日兜底） | R21 |
| I22 | 08-12 | R24 消息面缺龙虎榜 | 仅新闻/公告/事件 | 纳入东财龙虎榜（净买入/原因），情绪按净买入方向 | R24 |
| I23 | 08-12 | R25 缺止损/卖出计划 | LLM 输出未含买卖计划 | prompt 要求+字段归一化+基于买入价-3%止损/+3%目标兜底 | R25 |
| I24 | 08-12 | 昨日标的复盘在 HTML 一直看不到 | **报告服务为常驻进程，内存缓存旧版 HTML 模板**，改模板后未重启服务 | 重启 com.ashare.server；约定：HTML 模板变更后必须重启报告服务 | R09/R20 |
| I25 | 08-12 | 评估口径变更（用户决策） | 原按次日开盘卖，用户要求按昨收买→今收卖 | 结算 ret=今收/昨收-1，卡片/HTML 改显示昨收/今收/收益；规则 R10/R14 更新 v1.4 | R10/R14 |
| I26 | 08-12 | 手机打不开HTML（用户怀疑VPN） | 开VPN时系统多出 utun 虚拟网卡，原 socket 取IP可能返回VPN网卡IP | lan_ip 优先取默认路由物理网卡IP，排除 utun/tun；卡片同时给IP+.local | R09 |
| I27 | 08-13 | VPN每日开启+IP不固定，HTML链接手机常打不开 | 局域网链接依赖同一网络，VPN/跨网段失效 | 新增报告图片直发飞书（手机聊天内直接看图，完全绕开网络/IP）；HTML链接保留作同网交互版 | R09 |
| I28 | 08-13 | 加图片功能后整条复盘失败(NameError 'p') | 图片发送行引用未定义变量 p | 修复：_full_report_and_reply 内引入 paths 并用 _paths() 取报告路径 | C25/R29 |
| I29 | 08-13 | 报告图片上传失败(code 99991672) | 飞书应用缺 im:resource(获取与上传图片或文件资源) 权限 | 控制台补充权限+发布新版本；上传改用直接 requests 调 API | C27 |
| I30 | 08-13 | 图片已发送但用户看不到 | 图片卡片以"回复"形式发送，飞书折叠进回复区 | 改为新消息直发聊天(requests 调 message API) | C28 |
| I31 | 08-14 | HTML'昨日推荐复盘'不累计、只显示最近一天；后台需积累数据做分析 | 需求明确：前端不累计展示，后台做数据积累 | 新增 data/recommendation_history.json 积累+分析；HTML 跟踪表固定只显示最近一天 | C29 |

## 经验教训（工程化结论）
1. **外部依赖会变**：端口、域名、限流、网络——必须自动发现/多路兜底/显式上报，不能写死。
2. **一致性靠机制**："用户看到的=结算的""报告完整才复用"靠规则+代码强制，不靠记忆。
3. **合规靠自检**：规则系统能当场抓住未实现缺口与运行时异常（如 I11）。
4. **输出要双口径**：单一口径容易漏信息（开盘 vs 收盘）。
5. **工程基线化**：需求/架构/规则/问题记录全部文档化+git 版本化，变更走流程。
## I32: DeepSeek 研判解析失败

- **发现日期**: 2026-08-18
- **严重程度**: 中（飞书复盘输出标的为空）
- **现象**: 飞书发送"复盘"后，标的推荐显示"LLM研判失败"
- **根因**: DeepSeek API 返回 reasoning_content（推理过程）和 content（最终答案）两个字段。偶尔模型把推理文本混入 content，导致 safe_json 解析失败。json_mode=True 是提示而非保证，LLM 输出不可靠是固有特性。
- **修复**: safe_json 从2层容错增强到5层：直接解析 → 去围栏 → 截取{到} → 修复尾部逗号 → 提取最大完整JSON对象
- **提交**: 597eda6
- **测试**: 7/7 通过（正常JSON/带围栏/带推理文本/尾部逗号/嵌套JSON/空字符串/纯文本）
- **验证**: Bot 已重启，飞书连接正常
- **防复发**: safe_json 是防御性解析，未来 LLM 返回任意格式都能兜底

## I33: R29 合规自检系统失效

- **发现日期**: 2026-08-18
- **严重程度**: 高（整个合规自检系统不执行）
- **现象**: 代码修复后不记录日志也不会被发现，R29 规则形同虚设
- **根因**: 三层问题叠加
  1. R29 合规检查是空壳（rules.py:119 永远返回 True）
  2. _attach_compliance 引用未定义变量 pre → NameError → 整个合规自检静默失败
  3. 没有 git hook 强制检查
- **修复**:
  1. workflow.py: _attach_compliance 增加 pre 参数，调用处传入
  2. rules.py: R29 改为实际校验变更日志文件是否存在且非空
  3. .git/hooks/pre-commit: 代码变更时强制要求更新变更日志
- **测试**: _attach_compliance 正常执行 + R29 返回正确结果 + hook 文件可执行
- **防复发**: pre-commit hook 会在代码变更但未更新日志时阻止提交

## I34: 涨停股被推荐但无法买入

- **发现日期**: 2026-08-19
- **严重程度**: 高（核心策略缺陷，推荐标的46%是涨停股，实盘无法执行）
- **现象**: 历史推荐中7/15是涨停股（学大教育、宝光股份、联芸科技等），涨停板无法买入但被计入高收益
- **根因**: 三层防护缺失
  1. scoring.py: 涨停只扣5分，惩罚太轻
  2. strategy.py: 没有涨停过滤逻辑
  3. backtest.py: 只过滤一字板，不过滤普通涨停
- **修复**:
  1. scoring.py: 涨停扣分从-5改为-50（涨停股评分从~70降到~20）
  2. strategy.py: 候选池阶段直接过滤掉涨停股
  3. backtest.py: 回测跳过涨停股
- **测试**: 涨停股评分20.2 vs 正常股74.2，过滤有效
- **影响**: 历史命中率和收益率需要重新计算（去掉涨停股后会下降，但更真实）

## I35: 涨停不可交易硬约束规范化

- **发现日期**: 2026-08-19
- **严重程度**: 中（交易可执行性硬约束，非策略优化）
- **背景**: 系统会推荐涨停股票，但A股涨停股通常无法当日买入，属于"交易可执行性硬约束"
- **要求**: 涨停股票直接过滤，不进入最终推荐结果；保留候选池/评分数据供 RSI 研究
- **修改**:
  1. strategy.py: 新增 STRATEGY_VERSION="v1.1"，select() 过滤涨停并记录 filter_reason="limit_up_untradable"，用 self.filtered 保留被过滤股票(code/name/score/limit_status/filter_reason/strategy_version)
  2. daily.py: predict() 返回 strategy_version + filtered_out
  3. workflow.py: report["prediction"] 带 strategy_version + filtered_out
  4. scoring.py: 回滚上一轮 C39 误改的涨停扣分 -50 → -5（恢复原评分规则）
- **版本管理**: v1.0→v1.1，唯一变化是过滤涨停；旧记录无 version 字段=隐式 v1.0，不迁移不覆盖
- **RSI 兼容**: filtered_out 保留被过滤涨停股，未来可研究"若允许交易涨停股次日表现"
- **测试**: 32/32 通过（涨停过滤/评分回滚/量比不变/LLM不变/Top3/记录/RSI兼容/Legacy/RSI Flow/Phase2~6回归）
- **未做**: 未引入炸板回封/3~6%温和放量/T-1涨停低吸等未经数据验证的策略假设

## I34: safe_json 解析失败（单引号/混合内容）

- **发现日期**: 2026-08-24
- **严重程度**: 中（8-21 预测标的评分和逻辑为空）
- **现象**: 8-21 推荐的 3 只标的（四川黄金、百奥赛图、阳光诺和）评分和逻辑为空，market_view 显示"模型输出解析失败"
- **根因**: DeepSeek 返回的 JSON 可能包含单引号（Python 风格）或混合推理文本，safe_json 无法解析
- **修复**: 
  1. safe_json 新增第 6 层：单引号替换为双引号后解析
  2. safe_json 新增第 7 层：ast.literal_eval 解析 Python 字面量
  3. judge() 添加详细日志：记录 LLM 原始输出前 500 字符
  4. judge() 返回 raw_llm_output 字段：存储完整原始输出便于调试
  5. daily.py 存储 raw_llm_output：预测结果中保留原始 LLM 输出
- **测试**: 单引号/混合内容/空字符串等 7 种边界情况全部通过
- **验证**: 语法检查通过，功能测试通过
- **防复发**: safe_json 现有 7 层容错，覆盖更多 LLM 输出格式

## I36: 非交易日复盘失败 + 预测标的类型错误

- **发现日期**: 2026-08-24
- **严重程度**: 高（非交易日无法生成预测，影响用户体验）
- **现象**: 
  1. 今天（周一）复盘失败，预测状态为"预测生成失败"，错误信息 `'str' object has no attribute 'get'`
  2. 非交易日（周末）发送"复盘"时，系统返回提示但不生成预测
- **根因**: 
  1. judge.py 中 LLM 返回的 targets 列表可能包含非 dict 元素（字符串），导致 `.get()` 调用失败
  2. workflow.py 在非交易日时直接返回提示信息，不执行预测流程
  3. feishu bot 在非交易日发送警告后仍调用 `_full_report_and_reply`，但 `run_review` 已返回
- **修复**:
  1. judge.py: 添加 `isinstance(t, dict)` 类型检查，跳过非 dict 元素
  2. workflow.py: 非交易日时使用最近交易日继续执行，不返回提示
  3. feishu bot: 修改警告消息为"正在生成报告..."
  4. track.py: 添加 sell_date == pred_date 的异常处理
- **测试**: 待测试
- **防复发**: 类型检查 + 非交易日自动降级到最近交易日

## I37: MCP 返回类型错误导致结算失败

- **发现日期**: 2026-08-24
- **严重程度**: 高（结算时无法获取行情数据）
- **现象**: 结算时 MCP 返回字符串而非 dict，导致 `'str' object has no attribute 'get'` 错误
- **根因**: MCP 返回异常时 `parse_mcp_json()` 返回字符串，后续代码未做类型检查
- **修复**:
  1. track.py: 添加 `isinstance(resp, dict)` 类型检查，跳过非 dict 响应
  2. workflow.py: 添加同样的类型检查，避免 MCP 异常导致流程中断
- **测试**: 待测试
- **防复发**: 所有 MCP 调用处添加类型检查

## I38: MCP 缓存损坏导致预测流程崩溃

- **发现日期**: 2026-08-24
- **严重程度**: 高（复盘/预测无推荐标的）
- **现象**: 飞书发送"复盘"后，推荐标的为空，错误 `'str' object has no attribute 'get'`
- **根因**: 
  1. MCP 数据源临时异常时返回字符串，被 `parse_mcp_json` 原样写入缓存（1986 条全部损坏）
  2. 下游读取缓存时 `json.loads` 得到字符串而非 dict，调用 `.get()` 崩溃
- **修复**:
  1. 清空损坏的 mcp_cache.db（1986 条）
  2. app/predict/cache.py: `MCPCache.get()` 解析失败/非 dict 时返回 None（跳过缓存重拉）
  3. `CachedMcp.call()` 归一化：字符串尝试解析为 dict，失败返回空 dict（不缓存）
  4. 补 cache.py 缺失的 log 定义
  5. track.py/workflow.py 添加 MCP 响应类型检查
- **测试**: 完整复盘成功生成 3 只标的 + market_view 正常
- **防复发**: MCP 异常不再写入损坏缓存，非 dict 响应归一化

## I39: 候选池数值字段为字符串导致荐股失败

- **发现日期**: 2026-08-25
- **严重程度**: 高（荐股无标的，`bad operand type for abs(): 'str'`）
- **现象**: 飞书"复盘"后，推荐标的为空，错误 `bad operand type for abs(): 'str'`
- **根因**: MCP 数据源偶发返回字符串类型数值（change_ratio/amount 等），candidate_pool._is_valid 对字符串调用 abs()/比较运算崩溃
- **修复**:
  1. candidate_pool.py: 新增 `_safe_float()` 类型转换（兼容字符串/逗号/百分号）
  2. `_clean_rank_row()` 数值字段统一转 float（close/change_ratio/amount/turnover/market_cap/pe/pb/prev_close/volume）
  3. `_is_valid()` 使用 _safe_float 防御，避免 abs()/比较崩溃
- **测试**: 候选池构建成功（45个候选，change_ratio=float），预测生成3只标的
- **防复发**: 数值字段在入口统一转 float，数据源返回任何类型都能防御

## I40: LLM 研判偶发失败（Invalid JSON）

- **发现日期**: 2026-08-25
- **严重程度**: 中（market_view 显示解析失败，荐股回退规则候选）
- **现象**: market_view 显示"模型输出解析失败"，推荐标的 risk 显示"LLM研判失败"
- **根因**: DeepSeek json_object 模式在业务场景偶发返回 `{"error":"Invalid JSON"}` 或输出被截断（max_tokens 不足）
- **修复**:
  1. judge.py: max_tokens 5000→8000；解析失败自动用 medium effort 重试一次
  2. llm.py safe_json: 新增第8层"截断 JSON 恢复"（提取 market_view/完整 targets）
  3. daily.py: LLM 失败时降级——market_view 用实际市场环境生成中性判断（不暴露内部错误），risk 文案改为"规则策略推荐"
- **测试**: 完整预测成功生成 3 只标的（和元生物/益诺思/美迪西）+ 完整买卖计划 + market_view 正常
- **防复发**: 重试 + 截断恢复 + 降级三层保障

## I41: MCP K线查询漏掉 end_date 当天，结算误判无次日数据

- **发现日期**: 2026-08-26
- **严重程度**: 高（8-25 预测未在 8-26 结算，昨日复盘显示前日标的）
- **根因**: MCP get_kline_series 同时传 start_date+end_date 时是左闭右开，漏掉 end_date 当天
- **修复**: track.py settle_pending / backtest.py 两处，改用 end_date+limit，Python 侧过滤
- **防复发**: execution_rules.yaml 新增 R31，工程记忆 6c 记录

## I42: R11 未实现导致预测被覆盖，用户看到的≠结算的

- **发现日期**: 2026-08-26
- **严重程度**: 高（8-25 用户看到的和元生物等被 8-26 生成覆盖为康泰生物等）
- **根因**: workflow 生成预测前未检查该交易日是否已有预测，record_prediction upsert 覆盖
- **修复**: Tracker.get_prediction(date) + workflow 生成前复用检查
- **防复发**: R11 强化为"绝不覆盖"，工程记忆 6c 记录

## I43: MCP 缓存无 TTL 导致旧数据永久命中，交易日误判+预测覆盖

- **发现日期**: 2026-08-26
- **严重程度**: 高（8-26 复盘误判最新交易日=8-25，覆盖用户已看到的预测，结算错误标的）
- **根因**: 
  1. MCP 缓存无 TTL，某时刻缓存的 K 线数据（不含 8-26）永久命中
  2. trading_days 命中旧缓存 → 最新交易日误判为 8-25
  3. predict 生成 8-25 预测覆盖用户已看到的（和元生物等）→ R11 检查的是传入 trade_date 而非实际 t，未拦截
- **修复**:
  1. cache.py MCPCache.get() 加 TTL：当天缓存的命中，跨天自动过期重拉
  2. daily.py predict() R11 下沉：基于实际交易日 t（trading_days 算出）检查已有预测，有则复用
  3. 恢复被覆盖的 8-25 预测（和元生物/益诺思/诚达药业）并重新结算
- **验证**: trading_days 返回 8-26，昨日结算=和元生物等，今日预测=8-26 新标的
- **防复发**: 缓存 TTL + R11 基于实际 t 复用

## I44: 指数抓取失败（push2 主域名限流），自动复盘发不完整报告

- **发现日期**: 2026-08-26
- **严重程度**: 高（自动复盘发的报告大盘指数为 null）
- **根因**: 
  1. ego_scraper index 任务未用 _with_fallback（push2delay 兜底），push2 主域名被限流返回空 → 指数 null
  2. collector.collect 主流程未对 raw["index"] 做 _try_load 解析（字符串直接进 parse_index 会崩）
- **修复**:
  1. ego_scraper.py: index 任务加 _with_fallback（push2delay 优先）
  2. ego_scraper.py: parse_index 加字符串/None 防御
  3. collector.py: collect 主流程对 raw["index"] 用 _try_load 先解析
  4. run_review.sh: 加交易日检查（周末/节假日跳过自动复盘，避免发不完整数据）
- **验证**: 指数采集正常（上证3912.52等），报告完整
- **防复发**: index 走 push2delay 兜底 + 自动复盘仅交易日执行

## I45: 自动复盘凌晨误触发，生成"当天"错误报告

- **发现日期**: 2026-08-27
- **严重程度**: 高（00:21 凌晨自动复盘，生成 8-27 报告但数据是 8-26 的，半夜发飞书）
- **根因**: 
  1. run_review.sh 只判断"今天是否已复盘"，无时间窗口
  2. 8-27 凌晨触发时，脚本认为 8-27 未复盘 → 执行复盘 → 用 8-26 收盘数据生成"8-27 报告"
  3. get_latest_trading_day 返回"最近交易日(含今天)"，凌晨时错误返回当天
- **修复**:
  1. 新增 scripts/auto_review.py：计算"最近已收盘交易日"（今天过15:30且交易日→今天；否则从昨天往前找）
  2. run_review.sh 重写：插电检测 → 调 auto_review.py
  3. 删除 00:21 生成的错误 8-27 报告
- **验证**: 当前时间目标交易日=8-26（已复盘已发送→跳过），不再生成 8-27 报告
- **防复发**: 目标交易日=最近已收盘交易日，凌晨/盘前不会误触发

## I46: 缓存"当天有效"TTL 缺陷，8-27 整天命中凌晨旧数据，8-26 预测未结算

- **发现日期**: 2026-08-27
- **严重程度**: 高（昨日推荐复盘显示前天标的，8-26 预测未结算）
- **根因**: 
  1. 8-27 00:21 凌晨错误自动复盘时，缓存了不含 8-27 的 K 线数据
  2. cache.py TTL 是"当天有效"（created_at 是今天即命中），导致 8-27 整天命中凌晨旧数据
  3. settle_pending 查交易日拿不到 8-27 → 误判"8-26 之后无交易日" → 8-26 预测不结算
- **修复**:
  1. cache.py TTL 从"当天有效"改为"15分钟有效"（收盘后数据固定，15分钟足够）
  2. 清缓存后重新结算 8-26（康泰生物/智飞生物/华兰疫苗）
- **验证**: 昨日推荐复盘正确显示 8-26 三只标的，8-27 预测正常
- **防复发**: TTL=15分钟，凌晨缓存不会整天命中；即使命中，15分钟后自动重拉

## I47: "昨日推荐标的"显示前天标的（结算日期错位）

- **发现日期**: 2026-08-28
- **严重程度**: 高（用户每天最关注的核心功能，反复出现）
- **现象**: 8/28 复盘时，"昨日推荐标的"显示 8/26 的股票（前天），而非 8/27 的股票（昨天）
- **根因**: 
  1. 系统"昨日推荐标的"从 `stats.recent`（已结算记录）读取
  2. 8/27 的预测因 MCP K 线工具不可用导致结算失败
  3. 系统找不到 8/27 的结算记录 → 跳过 → 回退显示 8/26 的结算结果
- **修复**: 4 个文件统一改用 `latest_prediction`（最新预测记录，含未结算）：
  1. `app/workflow.py` `_build_tracking`: 增加 `latest_prediction` 字段
  2. `app/feishu/bot.py`: 飞书卡片用 `latest_prediction` 显示
  3. `scripts/send_review.py`: 自动复盘脚本同步
  4. `app/html_report.py`: HTML 报告同步
- **验证**: 8/28 报告 `tracking.latest_prediction.date=2026-08-27`，标的=敦煌种业/天山生物/长进光子
- **防复发**: 核心逻辑从"看结算记录"改为"看预测记录"，结算失败不影响显示

## I48: WorkBuddy MCP connector-proxy 屏蔽 tongzhou-fin-research fin_data 工具

- **发现日期**: 2026-08-28
- **严重程度**: 高（预测模块完全无法获取 K 线数据，预测/结算均失败）
- **现象**: connector-proxy 返回 "Tool not found in any connected connector"，但 WorkBuddy 内部测试正常
- **根因**: WorkBuddy catalog 机制要求 `ui_resourceUri` 元数据，`fin_data` 系列工具缺少该元数据被拒绝
- **影响**: 预测失败 → 结算失败 → "昨日推荐标的"无法显示收益
- **状态**: 待 WorkBuddy 侧修复，或研究绕过 connector-proxy 直连 MCP 服务器

---

### ISSUE-027：昨日推荐标的复盘显示今日标的 + 结算结果缺失
- **发现日期**：2026-08-28
- **严重程度**：高
- **现象**：飞书复盘报告的"昨日推荐标的复盘"栏显示的是今日（8-28）的推荐标的，而非昨日（8-27）的；且已结算的标的收益数据未展示
- **根因**：
  1. `_build_tracking()` 使用 `latest_prediction()` 返回最新预测（今日），未跳过
  2. `settle_pending()` 只查未结算预测，已结算的返回"无可结算"
  3. `_build_tracking()` 未主动查询 `prediction_results` 表的已结算数据
- **修复方案**：
  1. `_build_tracking()` 增加 `trade_date` 参数，跳过当日预测，取前一日
  2. 在获取 `latest_pred` 后，主动查询 `prediction_results` 表获取已结算结果
  3. 用已结算数据覆盖 `settle_result`，确保报告展示真实收益
- **修改文件**：`app/workflow.py`
- **修复版本**：v1.1
- **验证结果**：✅ 昨日标的正确显示（敦煌种业/天山生物/长进光子），结算收益正确（+10.05%/-7.75%/-6.28%）

---

### ISSUE-028：同类 bug 反复出现（日期/结算/推荐数量）
- **发现日期**：2026-08-28
- **严重程度**：中
- **现象**：ISSUE-027 等同类问题反复出现，缺乏系统性防御
- **根因**：无自动化校验、无回归测试、无结构化日志
- **修复方案**：建立三层防御体系
  - Layer 1：报告校验器（V01-V08），发送前自动拦截结构性错误
  - Layer 2：回归测试（18 个用例），每个 bug 对应一个永久测试
  - Layer 3：结构化日志（review_runs.jsonl），每次运行自动记录关键指标
- **修改文件**：新增 report_validator.py / review_logger.py / test_regression.py，修改 workflow.py / bot.py
- **验证结果**：✅ 18/18 测试通过，复盘运行 validation=PASS

---

### ISSUE-029：ths_client 导入路径错误导致预测失败
- **发现日期**：2026-08-31
- **严重程度**：高
- **现象**：8-31 复盘正常但推荐标的数量=0，V03 校验报错
- **根因**：`app/predict/daily.py` 使用 `from .ths_client import get_ths_client`（同级目录），但 `ths_client.py` 在 `app/` 目录
- **修复方案**：改为 `from ..ths_client import get_ths_client`（上级目录）
- **修改文件**：`app/predict/daily.py`（1行）
- **验证结果**：✅ 3只标的正常生成，校验通过

---

### ISSUE-030：16:00 定时复盘未推送，且盘中旧报告可能被误当终版
- **发现日期**：2026-09-02
- **严重程度**：高
- **现象**：已约定交易日 16:00 自动推送，但 16:00 未收到终版；当天上午生成、缺少收盘参考价的报告被当作“完整报告”跳过。
- **根因**：
  1. `hour >= 15 and minute >= 30` 的写法在 16:03 被误判为“未过 15:30”（分钟比较错误）。
  2. 自动任务只检查“报告存在且 3 只标的”，未检查报告是否在收盘后生成、是否含 T 日收盘参考价。
  3. `sent flag` 只判断存在，不绑定终版报告版本，旧 flag 会阻止终版补发。
  4. StartInterval 轮询可能在 16:00 前抢跑。
- **修复**：
  1. 时间判断改为完整 `time >= 16:00/15:30` 语义。
  2. 新增终版报告检查：收盘后生成 + 推荐完整 + T 日参考价齐全。
  3. 16:00 前轮询只等待；16:00 后错过则补执行。
  4. `sent flag` 写入并校验 `report.meta.generated_at`，盘中旧报告不会被误认为已终发。
  5. 推送前执行健康检查：同花顺或 MCP 任一可用即继续；双源失败先飞书告警并重试。
- **修改文件**：`scripts/auto_review.py`、`scripts/check_data_sources.py`、`scripts/send_feishu_alert.py`、`scripts/run_scheduled_review.sh`
- **测试**：24/24 回归通过；含 15:58 不抢跑、上午报告非终版、收盘后终版可推送。
- **防复发规则**：R15/R32 更新至 execution_rules v1.6。

---

### ISSUE-031：历史结算口径不是真实可执行窗口，8/31 评估错误
- **发现日期**：2026-09-02
- **严重程度**：高
- **现象**：T 日收盘后推荐，用户最早只能 T+1 买入；但历史收益按 T 日参考价买入、T+1 收盘卖出，导致 8/31 推荐被错误评估。
- **根因**：
  1. 结算口径未随“收盘后发布推荐”的真实交易约束升级。
  2. `_build_tracking()` 的 SELECT 列顺序与 Python 映射错位，日期/状态被显示成错误字段。
  3. 部分价格缺失时可能只结算部分标的。
- **修复**：
  1. `Tracker.settle()` 固定为 **T+1 收盘买入 → T+2 收盘卖出**，持久化 `buy_date/sell_date/reference_price`。
  2. `settle_pending()` 只有 T+2 卖出日出现后才结算；买/卖任一标的收盘价缺失则整体不结算，不伪造。
  3. 结算价格优先同花顺历史 K 线，MCP/ego 只作兜底。
  4. 修正跟踪详情字段映射。
  5. 新增 `scripts/recalculate_settlements.py`，先备份数据库，再逐笔从同花顺重拉历史收盘价重算。
- **验证**：12 个已完成交易日、36 笔全部重算；8/31 推荐为 9/1 收盘买入、9/2 收盘卖出：奥士康 -0.68%、锐科激光 -1.27%、光韵达 -1.90%；无空执行日期。
- **修改文件**：`app/predict/track.py`、`app/workflow.py`、`scripts/recalculate_settlements.py`
- **测试**：24/24 回归通过，含 T+1/T+2、重复结算去重、字段映射防串位。

---

### ISSUE-032：MCP 状态被 VPN/陈旧端口污染，告警和 launchd 不可靠
- **发现日期**：2026-09-02
- **严重程度**：高
- **现象**：WorkBuddy 本地 MCP 聚合端口实际未监听，但健康检查只报“TaskGroup unhandled errors”；开启 VPN 时连接可被系统代理改写成 502；中文项目路径导致 launchd 间歇性 `exit 127`。
- **根因**：
  1. MCP 客户端在本地端口全部失联时仍回退到陈旧端口。
  2. MCP HTTP 客户端继承系统代理环境，127.0.0.1 请求被 VPN/代理污染。
  3. MCP SDK 的 ExceptionGroup 被压缩成无运维价值的错误文本。
  4. launchd Plist 指向中文路径/数组曾含重复项，zsh 收到多余文件参数。
  5. 飞书告警文本直接拼接换行到 JSON 字符串，可能生成非法 content。
- **修复**：
  1. 本地代理发现只接受真实监听端口，失败时保留显式配置继续测试，不再回落陈旧端口。
  2. MCP HTTP 客户端强制 `trust_env=False`，本地与 MCP 请求不走系统/VPN代理。
  3. 健康检查解包 ExceptionGroup，返回真实 `ConnectError/HTTPStatusError`。
  4. launchd 改用 ASCII 稳定入口 `/Users/yage/ashare-scripts/run_ashare_review_v15.sh`，项目通过 `/Users/yage/ashare-project` 符号链接访问。
  5. 飞书告警 content 改为 `json.dumps()`，换行安全。
- **验证**：launchd RunAtLoad `exit 0`；健康检查 THS OK、MCP 显示真实连接失败；最终 9/2 报告已发飞书。
- **修改文件**：`app/mcp_client.py`、`scripts/check_data_sources.py`、`scripts/send_feishu_alert.py`
- **备注**：MCP 本地聚合端口未启动属于 WorkBuddy 运行态问题；系统已按规则飞书提示人工检查。

---

### ISSUE-033：WorkBuddy MCP聚合代理不可用，通达信无法兜底
- **发现日期**：2026-09-02
- **严重程度**：高
- **现象**：健康检查显示 WorkBuddy MCP 连接失败；项目内通达信调用仍只能依赖聚合代理，导致MCP兜底链整体不可用。
- **根因**：
  1. 通达信官方MCP需要OAuth Bearer和session管理，通用MCP SDK对其短连接SSE行为不稳定。
  2. 响应缺少charset时，HTTP库按ISO-8859-1解码，中文UTF-8可被拆出假换行，破坏SSE数据帧。
  3. initialized通知被服务端提前断连时，不应误判整个初始化失败。
  4. WorkBuddy与通达信直连缺少统一fallback工厂，散落在workflow/collector会漂移。
- **修复**：
  1. 新增 `app/tdx_mcp_client.py`：OAuth自动刷新、session管理、手工JSON-RPC、SSE按原始字节UTF-8解码、401刷新重试。
  2. 新增 `ResilientMcpClient`：WorkBuddy优先；通达信类工具自动直连；同舟K线契约归一化为`data.points`；非通达信能力不伪造兜底。
  3. `workflow/collector/sweep`统一使用`create_resilient_client()`。
  4. 健康检查新增`tdx_direct`；自动复盘门禁接受THS/WorkBuddy/TDX任一可用，降级仍告警。
  5. `data/tdx_oauth.json`保持600并加入`.gitignore`。
- **验证**：通达信直连`tools/list=19`；上证指数K线直连返回2026-08-31/09-01/09-02；上证指数直连行情可解析为收盘/涨跌幅/成交额；26/26回归通过。
- **修改文件**：`app/tdx_mcp_client.py`、`app/mcp_client.py`、`app/workflow.py`、`app/collector.py`、`app/predict/sweep.py`、`scripts/tdx_oauth_login.py`、`scripts/check_data_sources.py`、`scripts/auto_review.py`、`tests/test_tdx_fallback.py`。

---

### ISSUE-034：WorkBuddy失败后同舟消息面与Wind兜底仍不可直连
- **发现日期**：2026-09-02
- **严重程度**：中高
- **现象**：通达信直连兜底已上线，但同舟官方MCP和Wind官方MCP仍没有项目内授权通道；WorkBuddy代理不可用时，消息面/公告/新闻和Wind备援能力不可用。
- **根因**：
  1. 只有通达信有专用直连客户端，同舟/Wind没有统一OAuth/静态Bearer抽象。
  2. ResilientMcpClient只识别TDX工具和同舟K线契约，未处理同舟/Wind工具前缀。
  3. Wind官方OAuth元数据没有动态注册；WorkBuddy实际通过`${WIND_API_KEY}`注入Bearer。
- **修复**：
  1. 新增通用`DirectMcpClient`，支持OAuth授权码/refresh token与`static_bearer`。
  2. 新增`scripts/direct_mcp_oauth.py`，同舟/Wind可执行login/refresh/test。
  3. 同舟完成动态客户端注册与OAuth授权；实测16工具，刷新token成功。
  4. ResilientMcpClient统一注入TDX/同舟/Wind；WorkBuddy失败时按连接器前缀剥离并直连；缺失工具显式报错。
  5. 健康检查与自动复盘门禁新增`tongzhou_direct`、`wind_direct`。
- **验证**：同舟直连16工具；统一兜底真实调用`search_security`成功；刷新token成功；28/28 pytest通过。
- **当前限制**：Wind缺`WIND_API_KEY`或OAuth client凭据，健康状态显式FAIL；未伪造。
- **修改文件**：`app/direct_mcp_client.py`、`app/mcp_client.py`、`scripts/direct_mcp_oauth.py`、`scripts/check_data_sources.py`、`scripts/auto_review.py`、`tests/test_tdx_fallback.py`。
